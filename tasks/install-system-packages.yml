---
- name: Install essential packages using Homebrew (macOS)
  ansible.builtin.package:
    name:
      - curl
      - git
      - btop
      - wget
      - gcc
      - cmake
      - fzf
      - unzip
      - rsync
      - ripgrep
      - luarocks
      - python
      - neovim
      - node
      - tmux
      - gh
    state: present
  when: is_macos | default(false)

- name: Install essential packages using built-in package manager (Linux)
  ansible.builtin.package:
    name:
      - curl
      - git
      - btop
      - wget
      - gcc
      - cmake
      - fzf
      - unzip
      - rsync
      - ripgrep
      - luarocks
      - python3-pip
      - neovim
      - nodejs
      - tmux
      - gh
    state: present
    update_cache: true
  become: true
  when: not is_macos | default(false)

- name: Check if rustup is installed
  ansible.builtin.command: rustup --version
  register: rustup_check
  failed_when: false
  changed_when: false

- name: Install rustup
  ansible.builtin.shell: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  args:
    executable: /bin/bash
  when: rustup_check.rc != 0

- name: Check if tiktoken_core is installed
  ansible.builtin.shell: luarocks show tiktoken_core --local
  args:
    executable: /bin/bash
  register: tiktoken_check
  failed_when: false
  changed_when: false

- name: Install tiktoken_core via luarocks
  ansible.builtin.shell: |
    . ~/.cargo/env
    luarocks install tiktoken_core --local
  args:
    executable: /bin/bash
  when: tiktoken_check.rc != 0
